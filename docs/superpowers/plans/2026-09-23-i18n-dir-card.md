# LMS 启动器 i18n S2 · 目录卡片 实现计划

**日期：** 2026-09-23
**规格：** `specs/2026-09-23-i18n-dir-card-design.md`（决策与文案权威，本计划冲突时以规格为准）
**分支：** feat/i18n
**前置：** S0 基座已落地（`t()` / `dict` / `test-setup.ts` 固定 zh）；S1 已完成。
**涉及文件：** `src-main/i18n/dict.ts`、`src/modules/DirModule.vue`、`src/modules/DirModule.test.ts`。**不碰 App.vue。**

> TDD 节奏：任务 1–2 先写测试看红再实现；任务 3 后 `npm test` 必须全绿（既有 5 条 zh + 新 6 条 en + 词典一致性 + 全库其余）。
> commit 风格：`feat(i18n): …（S2）`，每任务独立提交（对齐 S1 历史）。

---

## 任务 1 · 词典入 6 个 dir.* key

**文件：** `src-main/i18n/dict.ts`

1. `zh` 表追加（值 = 现状中文串逐字不变，注意 `保存中…` 是全角 …，en 侧 `Saving...` 是半角三点）：
   - `'dir.title': 'llama.cpp 安装目录'`
   - `'dir.btn.select': '选择 llama.cpp 安装目录'`
   - `'dir.status.ok': 'llama-server.exe 已找到'`
   - `'dir.status.exe_missing': '未找到 llama-server.exe'`
   - `'dir.status.dir_missing': 'llama.cpp 安装目录不存在'`
   - `'dir.status.saving': '保存中…'`
2. `en` 表追加：
   - `'dir.title': 'llama.cpp directory'`
   - `'dir.btn.select': 'Select directory'`
   - `'dir.status.ok': 'llama-server.exe is available'`
   - `'dir.status.exe_missing': 'llama-server.exe not found'`
   - `'dir.status.dir_missing': "llama.cpp directory doesn't exist"`（值含单引号，用双引号字符串）
   - `'dir.status.saving': 'Saving...'`
3. 验证：`npx vitest run` 中词典一致性测试（zh/en key 集合相等、无空值）自动覆盖。

**提交：** `feat(i18n): 词典新增目录卡片 dir.* 6 key（S2）`

## 任务 2 · en 冒烟测试先行（红）

**文件：** `src/modules/DirModule.test.ts`（追加，不动既有 5 条）

新增独立 `describe('DirModule en 冒烟', …)`，模式对齐 `App.test.ts:832`（`applyLangLocal('en')`）：

1. 顶部 import：`import { applyLangLocal } from '../i18n';`；describe 内 `beforeEach(() => applyLangLocal('zh'))` 或末尾还原——**取 afterEach 还原 zh**，防污染同文件既有用例（vitest 同文件串行，但保险）。
2. 用例（复用既有 `mockLms` / `mounted` / `fire` / `statusText` 辅助函数；invoke mock 白名单在 `get_app_config` 基础上放行 `validate_dir` / `save_llama_dir`）：
   - ok 态：`fire({ status: 'ok', dir })` → `statusText` = `✓ llama-server.exe is available`
   - exe_missing 态：→ `✗ llama-server.exe not found`
   - dir_missing 态：→ `✗ llama.cpp directory doesn't exist`
   - unset 态：无任何 ok/error 行（与 zh 行为一致）
   - 标题+按钮：`w.find('h2').text()` 含 `llama.cpp directory`；`w.find('button.btn-dirpick')` 的 `data-tooltip` 与 `aria-label` 均 = `Select directory`
   - 保存中 en：**底线断言 = `expect(t('dir.status.saving')).toBe('Saving...')`**（`import { t, applyLangLocal } from '../i18n'`）。
     说明：组件未暴露 `validate()`，`pickDir` 走 `open_dir_dialog`（测试未 mock），保存中路径在组件测试内不可达；t() 直断等价覆盖词典 en 值，zh 侧 `保存中…` 由 S0 词典一致性测试兜底。执行者若发现更优雅的触发路径（如 mock `open_dir_dialog` resolve 目录）可升级为渲染级断言，但不阻塞。
3. 此时 `npm test` 预期**红**（组件仍输出中文串）。

**提交：** （并入任务 3 或独立 `test(i18n): DirModule en 冒烟 6 条先行（S2）`——**取独立提交**，保证红→绿可追溯）

## 任务 3 · DirModule.vue 接入 t()

**文件：** `src/modules/DirModule.vue`

1. `import { t } from '../i18n';`
2. `validate()`：`msg: 'llama-server.exe 已找到'` → `msg: 'dir.status.ok'`；`msg: '未找到 llama-server.exe'` → `msg: 'dir.status.exe_missing'`。
3. `onStartupLlamaCheck` 映射三处 msg 同改：`dir.status.ok` / `dir.status.exe_missing` / `dir.status.dir_missing`。
4. `status` ref 处加注释：`// msg 存词典 key（spec §3），模板 t() 即时重译`。
5. 模板：
   - `<h2>` 内 `llama.cpp 安装目录` → `{{ t('dir.title') }}`（svg 之后）
   - 按钮 `data-tooltip="选择 llama.cpp 安装目录" aria-label="选择 llama.cpp 安装目录"` → `data-tooltip="${{ t('dir.btn.select') }}" aria-label="${{ t('dir.btn.select') }}"`
   - 状态行 `✓ {{ status.msg }}` → `✓ {{ t(status.msg) }}`；`✗ {{ status.msg }}` → `✗ {{ t(status.msg) }}`
   - `保存中…` → `{{ t('dir.status.saving') }}`
6. `npm test` → **绿**（既有 5 条 zh 断言因 zh 下 t() = 原串天然不破 + 新 6 条 en 通过）。
7. `npm run build`（或项目对应 build 脚本）确认无类型/编译错误。

**提交：** `feat(i18n): 目录卡片 6 处文案接入 t()、状态行存 key 即时重译（S2）`

## 任务 4 · 全量验证 + 人工验收留档

1. `npm test` 全库全绿（预期用例数 = S1 完成时 522 + 6 = **528**，以实际为准并在留档中记录）。
2. 人工英文目视验收（用户执行，规格 §6/§7.6）：切 en 检查 dir 卡片标题/tooltip/4 态/保存中无溢出、无死 key 显示；切回 zh 零回归。
3. 更新分片全表 `2026-09-22-i18n-slices.md`：S2 行「独立 spec」列 → `✅ 已展开`（spec 文件名），状态 ☐ → ✅；§4 变更记录追加一行。

**提交：** `docs(i18n): S2 分片卡标记完成 + 变更记录`

## 任务 5 · 收尾（S10 回记 + 最终确认）

1. 在 S10 分片卡（`2026-09-22-i18n-slices.md` S10 条目）追加遗留项：**App.vue:107「目录校验 · …」两行日志**（zh：已找到/未找到 + dir 插值）纳入 S10 的 `log.*` key 化范围（规格 §8 R2），并提示 S10 需同步改 `App.test.ts` 对应 2 条断言（L194–212）。
2. 最终 `npm test` 确认全绿；`git status` 无未跟踪残留（.temp 等除外）。

**提交：** 随任务 4 的 docs 提交或独立 docs 提交。

---

## 执行注意

- **串行纪律**：S2 执行期间不要并行分派任何触及 App.vue 的分片（S8/S10）。
- **zh 零回归是硬验收**：6 条 zh 词典值逐字来自现状字符串，任务 3 前用 diff 核对。
- en 保存中用例若按任务 2 底线（t 直断）落地，留档注明；不阻塞验收。
- 词典一致性测试已存在（S0），不要新建重复测试文件。
