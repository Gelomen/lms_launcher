# i18n Slice 1（App 外壳）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 subagent-driven-development（推荐）或 executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 把 `src/App.vue` 的外壳 UI（品牌名、更新 pill、winbar 三键与 GitHub tooltip+aria、退出确认框 title/message）接入 S0 的 `t()`，在词典新增 `app` scope 的 12 个 key；`index.html` 维持 `lang="zh-CN"` 不改。

**架构：** 词典单一真源 `src-main/i18n/dict.ts` 新增 `app` scope；`App.vue` 只加一个 `import { t } from './i18n'`，把模板字面量替换为 `t(key, params)`；渲染端 `lang` ref 驱动即时重渲染。无新 IPC、无新依赖、无新 scope。

**技术栈：** Electron 28 + Vue 3 + TypeScript + Vitest 2（组件测试 happy-dom）。

**规格：** `docs/superpowers/specs/2026-09-23-i18n-app-shell-design.md`

## 全局约束

- 词典单一真源 `src-main/i18n/dict.ts`；**不引入第三方 i18n 依赖**。
- key 命名 `<scope>.<subject>[.<modifier>]`，全小写点分、2–4 段；本片只用 `app` scope（已在总纲 §3.2 白名单）。
- zh 值**逐字冻结**（见规格 §3），既有中文断言零改动。
- 不译边界（总纲 §1.2）：`LMS Launcher` 之外的专名 `llama-server`、`GitHub`；版本号 `v{version}`；`document.title` 恒定 `lms_launcher`。
- 英文按钮最短；下载态只显示百分比（总纲 §2）。
- 中文布局冻结；本片不改任何 CSS。
- `index.html` 不改（`git diff index.html` 必须为空）。
- 测试固定 zh（`src/test-setup.ts`）；**现有中文断言不改**。
- 每任务结尾 `npm test` 全绿；提交信息用中文 Conventional Commits。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `src-main/i18n/dict.ts`（修改） | 新增 `app.*` 12 key（zh/en） |
| `src-main/i18n/dict.test.ts`（修改） | 新增 S1 逐字值断言 |
| `src/App.vue`（修改） | `import { t }` + 模板 6 组文案 t() 化 |
| `src/App.test.ts`（修改） | 新增 `describe('i18n / App 外壳（Slice 1）')` |

---

### 任务 1：词典新增 `app.*`（12 key）

**文件：**
- 修改：`src-main/i18n/dict.ts`
- 修改：`src-main/i18n/dict.test.ts`

- [ ] **步骤 1：编写失败的测试**

在 `src-main/i18n/dict.test.ts` 的 `describe('i18n dict', ...)` 内追加：

```ts
  it('S1：app.* 的 zh 值与既有界面文案逐字一致', () => {
    expect(dict.zh['app.brand']).toBe('LMS 启动器');
    expect(dict.zh['app.winbar.github']).toBe('GitHub 仓库');
    expect(dict.zh['app.winbar.minimize']).toBe('最小化');
    expect(dict.zh['app.winbar.maximize']).toBe('最大化');
    expect(dict.zh['app.winbar.restore']).toBe('还原');
    expect(dict.zh['app.winbar.close']).toBe('关闭');
    expect(dict.zh['app.update.pill.available']).toBe('有新版本!');
    expect(dict.zh['app.update.pill.downloading']).toBe('下载中 {pct}%');
    expect(dict.zh['app.update.tip.available']).toBe('发现新版本 v{version}，点击查看并安装');
    expect(dict.zh['app.update.tip.downloading']).toBe('下载中 {pct}%，点击查看进度');
    expect(dict.zh['app.exit.title']).toBe('退出程序');
    expect(dict.zh['app.exit.message']).toBe('将停止 llama-server 并退出，是否确认？');
  });

  it('S1：app.* 的 en 值为最短文案（下载态仅百分比）', () => {
    expect(dict.en['app.brand']).toBe('LMS Launcher');
    expect(dict.en['app.winbar.github']).toBe('GitHub repository');
    expect(dict.en['app.winbar.minimize']).toBe('Minimize');
    expect(dict.en['app.winbar.maximize']).toBe('Maximize');
    expect(dict.en['app.winbar.restore']).toBe('Restore');
    expect(dict.en['app.winbar.close']).toBe('Close');
    expect(dict.en['app.update.pill.available']).toBe('New version!');
    expect(dict.en['app.update.pill.downloading']).toBe('{pct}%');
    expect(dict.en['app.update.tip.available']).toBe('Version {version} available, click to view and install');
    expect(dict.en['app.update.tip.downloading']).toBe('Downloading {pct}%, click to view progress');
    expect(dict.en['app.exit.title']).toBe('Exit');
    expect(dict.en['app.exit.message']).toBe('llama-server will be stopped. Continue?');
  });
```

- [ ] **步骤 2：运行测试确认失败**

运行：`npx vitest run src-main/i18n/dict.test.ts`
预期：新增 2 条用例失败，报 `undefined`（key 尚不存在）。

- [ ] **步骤 3：最小实现**

在 `src-main/i18n/dict.ts` 的 `zh` 块末尾（`'tray.tooltip.empty'` 之后）加：

```ts
    'app.brand': 'LMS 启动器',
    'app.winbar.github': 'GitHub 仓库',
    'app.winbar.minimize': '最小化',
    'app.winbar.maximize': '最大化',
    'app.winbar.restore': '还原',
    'app.winbar.close': '关闭',
    'app.update.pill.available': '有新版本!',
    'app.update.pill.downloading': '下载中 {pct}%',
    'app.update.tip.available': '发现新版本 v{version}，点击查看并安装',
    'app.update.tip.downloading': '下载中 {pct}%，点击查看进度',
    'app.exit.title': '退出程序',
    'app.exit.message': '将停止 llama-server 并退出，是否确认？',
```

在 `en` 块末尾（`'tray.tooltip.empty'` 之后）加：

```ts
    'app.brand': 'LMS Launcher',
    'app.winbar.github': 'GitHub repository',
    'app.winbar.minimize': 'Minimize',
    'app.winbar.maximize': 'Maximize',
    'app.winbar.restore': 'Restore',
    'app.winbar.close': 'Close',
    'app.update.pill.available': 'New version!',
    'app.update.pill.downloading': '{pct}%',
    'app.update.tip.available': 'Version {version} available, click to view and install',
    'app.update.tip.downloading': 'Downloading {pct}%, click to view progress',
    'app.exit.title': 'Exit',
    'app.exit.message': 'llama-server will be stopped. Continue?',
```

- [ ] **步骤 4：运行测试确认通过**

运行：`npx vitest run src-main/i18n/dict.test.ts`
预期：全绿（含既有「zh/en key 集合一致」「无空值」——新增 key 成对，自动通过）。

- [ ] **步骤 5：提交**

```bash
git add src-main/i18n/dict.ts src-main/i18n/dict.test.ts
git commit -m "feat(i18n): 词典新增 App 外壳 app.* 12 key（S1）"
```

---

### 任务 2：winbar 品牌 + GitHub / 三键 t() 化

**文件：**
- 修改：`src/App.test.ts`（新增 describe 与 winbar 用例）
- 修改：`src/App.vue`

- [ ] **步骤 1：编写失败的测试**

在 `src/App.test.ts` 顶部把 Vue 的 `nextTick` 加入导入（第 7 行附近）：

```ts
import { nextTick } from 'vue';
```

在文件末尾追加：

```ts
// ===== Slice 1（2026-09-23-i18n-app-shell-design）：App 外壳 i18n =====
describe('i18n / App 外壳（Slice 1）', () => {
  // S0 记录的坑：./i18n 必须延迟 import（静态 import 会在 vi.mock('./ipc') 注册前绑定真实 ipc）
  async function applyEn(): Promise<void> {
    const { applyLangLocal } = await import('./i18n');
    applyLangLocal('en');
    await nextTick();
  }

  function mountShell(): any {
    invoke.mockImplementation((cmd: string): Promise<unknown> => {
      switch (cmd) {
        case 'get_state': return Promise.resolve(READY);
        case 'get_configs': return Promise.resolve(cfg());
        default: return Promise.resolve(undefined);
      }
    });
    return mount(App);
  }

  it('winbar 品牌 / GitHub / 三键 tooltip+aria 随语言切换', async () => {
    const w = mountShell();
    await flush();
    expect(w.find('.winbar__name').text()).toBe('LMS 启动器');
    const btns = w.findAll('.winbtn');
    expect(btns[0].attributes('data-tooltip')).toBe('GitHub 仓库');
    await applyEn();
    expect(w.find('.winbar__name').text()).toBe('LMS Launcher');
    expect(btns[0].attributes('data-tooltip')).toBe('GitHub repository');
    expect(btns[0].attributes('aria-label')).toBe('GitHub repository');
    expect(btns[1].attributes('data-tooltip')).toBe('Minimize');
    expect(btns[1].attributes('aria-label')).toBe('Minimize');
    expect(btns[2].attributes('data-tooltip')).toBe('Maximize');
    expect(btns[2].attributes('aria-label')).toBe('Maximize');
    expect(btns[3].attributes('data-tooltip')).toBe('Close');
    expect(btns[3].attributes('aria-label')).toBe('Close');
    w.unmount();
  });

  it('已最大化时 en 还原键为 Restore', async () => {
    const w = mountShell();
    await flush();
    await applyEn();
    winMaxHandlers.at(-1)!({ maximized: true });
    await nextTick();
    const maxBtn = w.findAll('.winbtn')[2];
    expect(maxBtn.attributes('data-tooltip')).toBe('Restore');
    expect(maxBtn.attributes('aria-label')).toBe('Restore');
    w.unmount();
  });
});
```

- [ ] **步骤 2：运行测试确认失败**

运行：`npx vitest run src/App.test.ts`
预期：新增 2 条失败（品牌仍为 `LMS 启动器`、tooltip 仍为中文）。

- [ ] **步骤 3：最小实现**

`src/App.vue` script 段：在 `import SettingsModal from './modules/SettingsModal.vue';` 之后加

```ts
import { t } from './i18n';
```

`src/App.vue` 模板段按下表替换（只动属性/文本，不动结构与 class）：

| 现状 | 改为 |
|---|---|
| `<span class="winbar__name">LMS 启动器</span>` | `<span class="winbar__name">{{ t('app.brand') }}</span>` |
| GitHub 键 `data-tooltip="GitHub 仓库" aria-label="GitHub 仓库"` | `:data-tooltip="t('app.winbar.github')" :aria-label="t('app.winbar.github')"` |
| 最小化键 `data-tooltip="最小化" aria-label="最小化"` | `:data-tooltip="t('app.winbar.minimize')" :aria-label="t('app.winbar.minimize')"` |
| 最大化键 `:data-tooltip="maximized ? '还原' : '最大化'" :aria-label="maximized ? '还原' : '最大化'"` | `:data-tooltip="t(maximized ? 'app.winbar.restore' : 'app.winbar.maximize')" :aria-label="t(maximized ? 'app.winbar.restore' : 'app.winbar.maximize')"` |
| 关闭键 `data-tooltip="关闭" aria-label="关闭"` | `:data-tooltip="t('app.winbar.close')" :aria-label="t('app.winbar.close')"` |

- [ ] **步骤 4：运行测试确认通过**

运行：`npm test`
预期：全绿（既有中文断言因 zh 值逐字一致而保持不变）。

- [ ] **步骤 5：提交**

```bash
git add src/App.vue src/App.test.ts
git commit -m "feat(i18n): App 外壳 winbar 品牌与三键 tooltip/aria 接入 t()（S1）"
```

---

### 任务 3：更新 pill t() 化

**文件：**
- 修改：`src/App.test.ts`（在任务 2 的 describe 内追加）
- 修改：`src/App.vue`

- [ ] **步骤 1：编写失败的测试**

在 `describe('i18n / App 外壳（Slice 1）')` 内追加：

```ts
  function mountWithUpdate(): any {
    invoke.mockImplementation((cmd: string): Promise<unknown> => {
      switch (cmd) {
        case 'get_state': return Promise.resolve(READY);
        case 'get_configs': return Promise.resolve(cfg());
        case 'check_update': return Promise.resolve({ available: true, status: 'update-available', version: '9.9.9' });
        default: return Promise.resolve(undefined);
      }
    });
    return mount(App);
  }

  it('更新 pill available 态英文最短文案 + tooltip', async () => {
    const w = mountWithUpdate();
    await flush();
    await applyEn();
    const pill = w.find('.update-pill');
    expect(pill.text()).toBe('New version!');
    expect(pill.attributes('data-tooltip')).toBe('Version 9.9.9 available, click to view and install');
    w.unmount();
  });

  it('更新 pill downloading 态英文只显示百分比 + tooltip', async () => {
    const w = mountWithUpdate();
    await flush();
    await applyEn();
    updateProgressHandlers.at(-1)!({ pct: 55 });
    await nextTick();
    const busy = w.find('.update-pill--busy');
    expect(busy.text()).toBe('55%');
    expect(busy.attributes('data-tooltip')).toBe('Downloading 55%, click to view progress');
    w.unmount();
  });
```

- [ ] **步骤 2：运行测试确认失败**

运行：`npx vitest run src/App.test.ts`
预期：新增 2 条失败（仍渲染中文 `有新版本!` / `下载中 55%`）。

- [ ] **步骤 3：最小实现**

`src/App.vue` 模板段：

| 现状 | 改为 |
|---|---|
| available tooltip `:data-tooltip="'发现新版本 v' + updateState.version + '，点击查看并安装'"` | `:data-tooltip="t('app.update.tip.available', { version: updateState.version })"` |
| available 文本 `>有新版本!</button>` | `>{{ t('app.update.pill.available') }}</button>` |
| downloading tooltip `:data-tooltip="'下载中 ' + updateState.pct + '%，点击查看进度'"` | `:data-tooltip="t('app.update.tip.downloading', { pct: updateState.pct })"` |
| downloading 文本 `>下载中 {{ updateState.pct }}%</button>` | `>{{ t('app.update.pill.downloading', { pct: updateState.pct }) }}</button>` |

- [ ] **步骤 4：运行测试确认通过**

运行：`npm test`
预期：全绿（含既有第 573/576/605/609 行的中文 pill 断言）。

- [ ] **步骤 5：提交**

```bash
git add src/App.vue src/App.test.ts
git commit -m "feat(i18n): 顶栏更新 pill 接入 t()（最短英文/下载态仅百分比）（S1）"
```

---

### 任务 4：退出确认框 title/message t() 化

**文件：**
- 修改：`src/App.test.ts`（在任务 2 的 describe 内追加）
- 修改：`src/App.vue`

- [ ] **步骤 1：编写失败的测试**

在 `describe('i18n / App 外壳（Slice 1）')` 内追加：

```ts
  it('退出确认框 title/message 随语言即时重译', async () => {
    const w = mountShell();
    await flush();
    trayHandlers.at(-1)!(); // 托盘「退出」→ 打开确认框
    await nextTick();
    expect(document.querySelector('.confirm-box')!.textContent).toContain('退出程序');
    await applyEn();
    await flush();
    const box = document.querySelector('.confirm-box') as HTMLElement;
    expect(box.textContent).toContain('Exit');
    expect(box.textContent).toContain('llama-server will be stopped. Continue?');
    w.unmount();
  });
```

- [ ] **步骤 2：运行测试确认失败**

运行：`npx vitest run src/App.test.ts`
预期：新增用例失败（英文下仍显示 `退出程序`）。

- [ ] **步骤 3：最小实现**

`src/App.vue` 模板段把

```html
<ConfirmDialog :open="exitConfirm" title="退出程序" message="将停止 llama-server 并退出，是否确认？" tone="primary"
```

改为

```html
<ConfirmDialog :open="exitConfirm" :title="t('app.exit.title')" :message="t('app.exit.message')" tone="primary"
```

> `ConfirmDialog.vue` 内部的 `取消`/`确认` 按钮归 S9，本任务不动。

- [ ] **步骤 4：运行测试确认通过**

运行：`npm test`
预期：全绿（含既有第 642 行 `退出程序` 断言）。

- [ ] **步骤 5：提交**

```bash
git add src/App.vue src/App.test.ts
git commit -m "feat(i18n): 退出确认框 title/message 接入 t()（S1）"
```

---

### 任务 5：验证、人工布局验收与看板收尾

**文件：**
- 修改：`docs/superpowers/specs/2026-09-22-i18n-slices.md`（S1 状态 ◐ → ✅）

- [ ] **步骤 1：全量测试**

运行：`npm test`
预期：全绿，无新增告警（`[i18n] missing key` 不应出现）。

- [ ] **步骤 2：确认 index.html 未改**

运行：`git diff --stat index.html`
预期：无输出（空 diff）。

- [ ] **步骤 3：人工英文布局验收**

启动 `npm run dev`，在设置弹窗切到 English，检查：

1. winbar：`LMS Launcher` + 版本号 + `New version!`（或 `{pct}%`）+ GitHub/三键不换行、不重叠。
2. 托盘「退出」弹出的 `.confirm-box`（360px）：`Exit` / `llama-server will be stopped. Continue?` 不溢出。
3. 切回中文，确认界面与切换前一致。

（happy-dom 无布局计算，故本项只能人工/DOM 目视；规格 §8.6。）

- [ ] **步骤 4：看板收尾**

把 `2026-09-22-i18n-slices.md` 中 S1 状态由 ◐ 改为 ✅，并在 §4 变更记录追加一行实现完成说明。

- [ ] **步骤 5：提交**

```bash
git add docs/superpowers/specs/2026-09-22-i18n-slices.md
git commit -m "docs(i18n): S1 分片卡标记完成"
```

---

## 完成后

- S1 卡状态 ✅ 后，可继续 S2–S11（彼此只依赖 S0，可任意顺序）。
- `App.vue` 的 S8/S10 字符串仍待后续分片处理（§1.2 边界）。
